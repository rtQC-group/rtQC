% rtQC_log_timing
% Times latency between TTL scanner pulse (send during acquisition of 1st slice of each volume) and image availability for real-time processing. Must work for all types of possible TTL communication (USB, serial, parallel). Calculates mean latency, range and standard deviation for entire sequence.
% Current developers: Stavros
